import {Router} from 'express';


const route = Router();

//POST /users/login
route.post("/", (req,res) => {

});

//POST /users Register a new user
route.post("/", (req,res) => {

});


export const usersRoute = route;